jdk:
  - openjdk11
before_install:
  - chmod +x gradlew
install:
  - ./gradlew openApiGenerate
  - ./gradlew fixGeneratedCode
  - ./gradlew publishToMavenLocal -x javadoc -x openApiGenerate -x fixGeneratedCode